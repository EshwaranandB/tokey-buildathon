# Razorpay integration boundary

This directory contains public request/response schemas for the Razorpay harness boundary. The full browser demo UI, credentials, provider secrets, database, settlement verifier, and payment runtime are intentionally excluded.

The verified proof is documented in [`docs/RAZORPAY_PROOF.md`](../../docs/RAZORPAY_PROOF.md).
