# OpenClaw / MCP proof

Tokey's agent-runtime proof is distinct from its Razorpay payment-rail proof.

```text
Telegram
→ TON
→ OpenClaw
→ Tokey MCP
→ Tokey Core
```

The verified integration demonstrates an agent runtime inspecting its constrained financial authority through Tokey's MCP surface. It does not claim that TON itself originated the canonical ₹1,799 Razorpay Test Mode transaction.

MCP calls resolve an authenticated Tokey actor. Tokey, not a tool argument, enforces tenant scope, role permissions, and agent identity binding.
