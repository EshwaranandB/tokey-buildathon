# Optional browser-to-Core endpoint. Empty keeps development requests same-origin.
VITE_TOKEY_API_URL=

# Development proxy target only. Never place credentials in this file.
TOKEY_API_PROXY_TARGET=http://127.0.0.1:8787
