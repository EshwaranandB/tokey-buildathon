import { Webhook } from "lucide-react";
import { Badge } from "../components/primitives/Badge";
import { Card, SectionHeader } from "../components/primitives/Card";

export function WebhooksPage() {
  return <div className="mx-auto max-w-5xl space-y-5"><SectionHeader title="Webhooks" description="Provider events are verified before they can affect Tokey settlement state." /><Card><div className="flex items-start gap-3"><Webhook className="mt-0.5 h-4 w-4 text-ink-3" /><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><div className="text-xs font-semibold text-ink">Razorpay</div><Badge tone="ok" dot>Signed verification</Badge></div><dl className="mt-4 divide-y divide-line text-xs"><div className="flex justify-between gap-5 py-2.5"><dt className="text-ink-3">Provider event handling</dt><dd className="font-medium text-ink">Configured</dd></div><div className="flex justify-between gap-5 py-2.5"><dt className="text-ink-3">Verification</dt><dd className="font-medium text-ink">Raw-body HMAC before settlement</dd></div><div className="flex justify-between gap-5 py-2.5"><dt className="text-ink-3">Replay handling</dt><dd className="font-medium text-ink">Exactly-once capture protection</dd></div></dl><p className="mt-4 text-[11px] leading-5 text-ink-3">Webhook URLs and secrets are deliberately not displayed. Provider evidence is validated against the persisted reservation and expected order context.</p></div></div></Card></div>;
}
